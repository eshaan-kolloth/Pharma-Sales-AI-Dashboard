name: Keep app awake

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Visit the app
        run: curl -sS --max-time 30 https://pharma-sales-ai-dashboard-bmjpnuwwjtmz6brgqntckf.streamlit.app/ || true
